# Engram

> __engram (noun)__: a hypothetical permanent change in the brain accounting for the existence of memory; a memory trace.

## Testing

```sh
# all tests
uv run pytest -v
# single file
uv run pytest tests/engram/test_buffer.py -v
```
